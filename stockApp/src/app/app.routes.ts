import { Routes } from '@angular/router';
import { StockManagemetComponent } from './components/stock-managemet/stock-managemet.component';

export const routes: Routes = [
    {
        path: 'management',
        component: StockManagemetComponent,
    },
];
